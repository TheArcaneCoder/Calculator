package com.thearcanecoder.calculator.data.enums

enum class ButtonType(val type: Int) {
    NUMBER(0),
    OPERATOR(1),
    EVALUATE(2),
    DECIMAL(3),
    CLEAR(4),
    OPEN_PARENTHESIS(5),
    CLOSE_PARENTHESIS(6)
}